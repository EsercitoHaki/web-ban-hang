api:
  prefix: api/v1
baseUrl: http://localhost:8088
jwt:
  expiration: 2592000
  secretKey: Nq1t86/HZmr1Z8rFY/Smm3JUQxtUnHeahOHs+A5BVvk=
server:
  port: 8088
spring:
  profiles:
    active: default

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ''
    url: jdbc:mysql://localhost:3306/WebBanHang?useSSL=false&serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
  messages:
    basename: i18n/messages
    default-locale: en
    encoding: UTF-8
  security:
    oauth2:
      client:
        registration:
          facebook:
            client-id: YOUR_FACEBOOK_APP_ID
            client-secret: YOUR_FACEBOOK_APP_SECRET
            redirect-uri: '{baseUrl}/login/oauth2/code/facebook'
            scope: public_profile,email
          google:
            client-id: YOUR_FACEBOOK_APP_ID
            client-secret: YOUR_FACEBOOK_APP_ID
            redirect-uri: '{baseUrl}/login/oauth2/code/google'
            scope: profile,email
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


